# Frozen choices made on Noisy DEV only. This file contains no machine paths.
seed: 1986

fsq:
  base: 3
  digits: 8
  vocabulary_size: 6561

pool_d:
  candidate_order:
    - full
    - first
    - middle
    - final
    - tfmap_context_full
  selection: maximum_target_enrollment_cosine
  tie_break: first_in_candidate_order

fixed_csg:
  lambda: 1.0
  temporal_tolerance: 0

adaptive_csg_ablation:
  selected_policy: source_threshold_difficulty_conditioned
  temporal_tolerance: 1
  thresholds_db: [-3.013, -1.640, -0.267, 1.179, 2.697, 4.216, 7.515, 13.158]
  lambdas: [2.0, 1.75, 1.5, 1.25, 1.0, 0.75, 0.5, 0.25, 0.0]
  interpretation: difficulty_feature_not_validated_as_an_snr_estimator

gnr_ablation:
  selected_within_gnr:
    top_k: 20
    hamming_radius: 2
  history: immutable_complete_anchor_prefix
  refined_tokens_fed_back: false
